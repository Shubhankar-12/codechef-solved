#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int x1, y1, x2, y2;
        cin >> x1 >> y1 >> x2 >> y2;
        int d1 = sqrt(x1 * x1 + y1 * y1);
        int d2 = sqrt(x2 * x2 + y2 * y2);
    }

    return 0;
}